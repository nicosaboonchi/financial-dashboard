## Getting Started

1. Run npm install to download necessary packages.
2. Copy the .env.example file to .env.local and enter associated API keys
3. Run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
